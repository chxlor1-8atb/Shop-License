# ===================================
# Database (Supabase PostgreSQL)
# ===================================
# Connection string จาก Supabase Dashboard > Project Settings > Database > Connection string
# ใช้ "Transaction" mode (port 6543) สำหรับ serverless (Vercel)
# หรือ "Session" mode (port 5432) สำหรับ long-lived connections
DATABASE_URL=postgresql://postgres.[project-ref]:[password]@aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres

# ===================================
# Session Encryption (iron-session)
# ===================================
SESSION_SECRET=your-secret-key-at-least-32-characters-long

# ===================================
# Optional
# ===================================
# ALLOW_MIGRATE=true  # Enable migration endpoint (dev only)
