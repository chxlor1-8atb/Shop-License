# 📋 Phase 3: คู่มือการทดสอบ Shops CRUD

## สถานะการทดสอบ

| ลำดับ | รายการทดสอบ | สถานะ | หมายเหตุ |
|------|------------|-------|----------|
| 1 | ทดสอบสร้างร้านค้าพร้อม Custom Fields | ✅ สำเร็จ | ทดสอบแล้วเมื่อ 27 ม.ค. 2026 เวลา 15:40 น. |
| 2 | ทดสอบแก้ไขข้อมูลร้านค้า | ⏳ รอทดสอบ | ดูคำแนะนำด้านล่าง |
| 3 | ทดสอบลบร้านค้า | ⏳ รอทดสอบ | ดูคำแนะนำด้านล่าง |
| 4 | ทดสอบการค้นหาร้านค้า | ⏳ รอทดสอบ | ดูคำแนะนำด้านล่าง |
| 5 | ทดสอบ Pagination | ⏳ รอทดสอบ | ดูคำแนะนำด้านล่าง |

---

## ✅ 1. ทดสอบสร้างร้านค้าพร้อม Custom Fields

### ผลการทดสอบ: **สำเร็จ** ✅

#### ข้อมูลที่ใช้ทดสอบ:
- **ชื่อร้านค้า:** ร้านกาแฟอรุณสวัสดิ์
- **ชื่อเจ้าของ:** นางสาวสุดา ใจดี
- **เบอร์โทรศัพท์:** 089-123-4567
- **ที่อยู่:** 456 ถนนสุขุมวิท กรุงเทพฯ
- **Custom Field - เลขประจำตัวผู้เสียภาษี:** 1234567890123

#### Custom Fields ที่พบในฟอร์ม:
1. ✅ **เลขประจำตัวผู้เสียภาษี** (Required) - Text
2. ✅ **ประเภทธุรกิจ** - Select
3. ✅ **วันที่จดทะเบียน** - Date
4. ✅ **จำนวนพนักงาน** - Number
5. ✅ **รายละเอียดธุรกิจ** - Textarea
6. ✅ **คอลัมน์ใหม่** - Text

#### ผลลัพธ์:
- ✅ แสดงข้อความ "สร้างร้านค้าเรียบร้อย"
- ✅ ร้านค้าใหม่ปรากฏในตารางที่ลำดับ 1
- ✅ Custom fields ถูกบันทึกและแสดงในตาราง

#### ⚠️ ข้อสังเกต:
- ❌ **ไม่พบฟิลด์ "อีเมล" (Email)** ในฟอร์มสร้างร้านค้า แม้ว่าจะมีคอลัมน์นี้ในตาราง
  - **แนะนำ:** ควรเพิ่มฟิลด์ Email ในฟอร์ม QuickAddModal

---

## ⏳ 2. ทดสอบแก้ไขข้อมูลร้านค้า

### วิธีการทดสอบ:

ระบบใช้ **ExcelTable** ซึ่งสามารถแก้ไขข้อมูลได้โดยตรงในตาราง (Inline Editing)

#### ขั้นตอนการทดสอบ:

1. **ไปที่หน้า Dashboard > Shops** (`http://localhost:3000/dashboard/shops`)

2. **ค้นหาร้านค้า "ร้านกาแฟอรุณสวัสดิ์"** ที่เพิ่งสร้างไว้

3. **ทดสอบแก้ไขฟิลด์มาตรฐาน:**
   - คลิกที่เซลล์ "เบอร์โทรศัพท์" 
   - เปลี่ยนจาก `089-123-4567` เป็น `089-999-8888`
   - กด Enter หรือคลิกนอกเซลล์เพื่อบันทึก
   - **ตรวจสอบ:** ระบบควรบันทึกอัตโนมัติและไม่มี error

4. **ทดสอบแก้ไขฟิลด์ที่อยู่:**
   - คลิกที่เซลล์ "ที่อยู่"
   - เปลี่ยนเป็น `789 ถนนพระราม 4 กรุงเทพฯ 10500`
   - กด Enter
   - **ตรวจสอบ:** ข้อมูลควรถูกบันทึก

5. **ทดสอบแก้ไข Custom Fields:**
   - คลิกที่เซลล์ "เลขประจำตัวผู้เสียภาษี"
   - เปลี่ยนเป็น `9876543210987`
   - กด Enter
   - **ตรวจสอบ:** Custom field ควรถูกอัพเดท

6. **ทดสอบแก้ไขฟิลด์ตัวเลข:**
   - คลิกที่เซลล์ "จำนวนพนักงาน"
   - เปลี่ยนเป็น `10`
   - กด Enter
   - **ตรวจสอบ:** ตัวเลขควรถูกบันทึก

7. **รีเฟรชหน้าเว็บ (F5)** และตรวจสอบว่าข้อมูลที่แก้ไขยังคงอยู่

### เกณฑ์การผ่าน:
- ✅ สามารถแก้ไขฟิลด์มาตรฐานได้
- ✅ สามารถแก้ไข Custom Fields ได้
- ✅ ข้อมูลถูกบันทึกในฐานข้อมูล (ยังคงอยู่หลังรีเฟรช)
- ✅ ไม่มี error ใน Console
- ✅ แสดงข้อความสำเร็จ (ถ้ามี)

### ข้อมูลทางเทคนิค:
- **API Endpoint:** `PUT /api/shops`
- **Function:** `handleRowUpdate` ใน `src/app/dashboard/shops/page.jsx`
- **Payload Structure:**
```json
{
  "id": 1,
  "shop_name": "ร้านกาแฟอรุณสวัสดิ์",
  "owner_name": "นางสาวสุดา ใจดี",
  "phone": "089-999-8888",
  "address": "789 ถนนพระราม 4 กรุงเทพฯ 10500",
  "email": null,
  "notes": null,
  "custom_fields": {
    "cf_tax_id": "9876543210987",
    "cf_employee_count": "10"
  }
}
```

---

## ⏳ 3. ทดสอบลบร้านค้า

### วิธีการทดสอบ:

#### ขั้นตอนการทดสอบ:

1. **ไปที่หน้า Dashboard > Shops**

2. **เลือกร้านค้าที่ต้องการลบ** (แนะนำให้สร้างร้านค้าทดสอบใหม่ก่อน)

3. **ค้นหาปุ่มลบ:**
   - มองหาไอคอนถังขยะ (🗑️) หรือปุ่ม "ลบ" ในแถวของตาราง
   - หรือคลิกขวาที่แถวเพื่อดูเมนู context

4. **คลิกปุ่มลบ**

5. **ยืนยันการลบ:**
   - ระบบควรแสดง confirmation dialog
   - คลิก "ยืนยัน" หรือ "ลบ"

6. **ตรวจสอบผลลัพธ์:**
   - ร้านค้าควรหายไปจากตาราง
   - แสดงข้อความสำเร็จ
   - จำนวนรายการทั้งหมดลดลง

7. **รีเฟรชหน้าเว็บ** เพื่อยืนยันว่าข้อมูลถูกลบจริง

### เกณฑ์การผ่าน:
- ✅ มี confirmation dialog ก่อนลบ
- ✅ ร้านค้าถูกลบออกจากตาราง
- ✅ ข้อมูลถูกลบจากฐานข้อมูล
- ✅ แสดงข้อความสำเร็จ
- ✅ ไม่มี error

### ⚠️ ข้อควรระวัง:
- **ห้ามลบร้านค้าที่มีใบอนุญาตอยู่** (ควรมีการป้องกัน)
- ควรทดสอบกรณี:
  - ลบร้านค้าที่ไม่มีใบอนุญาต ✅ ควรลบได้
  - ลบร้านค้าที่มีใบอนุญาต ❌ ควรแสดง error

### ข้อมูลทางเทคนิค:
- **API Endpoint:** `DELETE /api/shops`
- **Function:** `handleRowDelete` ใน `src/app/dashboard/shops/page.jsx`

---

## ⏳ 4. ทดสอบการค้นหาร้านค้า

### วิธีการทดสอบ:

#### ขั้นตอนการทดสอบ:

1. **ไปที่หน้า Dashboard > Shops**

2. **ดูช่องค้นหา** ที่มี placeholder "ชื่อร้าน, เจ้าของ, เบอร์โทร..."

3. **ทดสอบค้นหาด้วยชื่อร้าน:**
   - พิมพ์ "กาแฟ" ในช่องค้นหา
   - **ตรวจสอบ:** ควรแสดงเฉพาะร้านที่มีคำว่า "กาแฟ" ในชื่อ

4. **ทดสอบค้นหาด้วยชื่อเจ้าของ:**
   - ล้างช่องค้นหา
   - พิมพ์ "สุดา"
   - **ตรวจสอบ:** ควรแสดงร้านที่เจ้าของชื่อ "สุดา"

5. **ทดสอบค้นหาด้วยเบอร์โทร:**
   - ล้างช่องค้นหา
   - พิมพ์ "089"
   - **ตรวจสอบ:** ควรแสดงร้านที่มีเบอร์ขึ้นต้นด้วย 089

6. **ทดสอบค้นหาบางส่วน (Partial Search):**
   - พิมพ์ "อรุณ"
   - **ตรวจสอบ:** ควรแสดงร้าน "ร้านกาแฟอรุณสวัสดิ์"

7. **ทดสอบค้นหาที่ไม่พบผล:**
   - พิมพ์ "ไม่มีร้านนี้แน่นอน"
   - **ตรวจสอบ:** ควรแสดงข้อความ "ไม่พบข้อมูล" หรือตารางว่าง

8. **ทดสอบล้างการค้นหา:**
   - ล้างช่องค้นหา
   - **ตรวจสอบ:** ควรแสดงร้านค้าทั้งหมดกลับมา

### เกณฑ์การผ่าน:
- ✅ ค้นหาด้วยชื่อร้านได้
- ✅ ค้นหาด้วยชื่อเจ้าของได้
- ✅ ค้นหาด้วยเบอร์โทรได้
- ✅ รองรับการค้นหาบางส่วน (Partial Match)
- ✅ แสดงผลลัพธ์ทันที (Real-time Search)
- ✅ ล้างการค้นหาแสดงข้อมูลทั้งหมดกลับมา

### ข้อมูลทางเทคนิค:
- **Search Logic:** ใน `performFetchShops` function
- **API:** `GET /api/shops?search=<keyword>`

---

## ⏳ 5. ทดสอบ Pagination

### วิธีการทดสอบ:

#### เตรียมข้อมูล:
ก่อนทดสอบ Pagination ควรมีร้านค้าอย่างน้อย **15-20 รายการ** 

**วิธีสร้างข้อมูลทดสอบเพิ่ม:**
1. ใช้ปุ่ม "สร้างร้านค้าใหม่" สร้างร้านเพิ่มอีก 10-15 ร้าน
2. หรือใช้ Seed API (ถ้ามี): `GET /api/seed-shops`

#### ขั้นตอนการทดสอบ:

1. **ไปที่หน้า Dashboard > Shops**

2. **ตรวจสอบ Pagination Controls:**
   - ดูที่ด้านล่างของตาราง
   - ควรมี:
     - ตัวเลือก "แสดง X รายการต่อหน้า" (10, 25, 50, 100)
     - ปุ่ม "ก่อนหน้า" / "ถัดไป"
     - แสดงข้อความ "1-10 จาก 20 รายการ"

3. **ทดสอบเปลี่ยนจำนวนรายการต่อหน้า:**
   - เลือก "10 รายการต่อหน้า"
   - **ตรวจสอบ:** ควรแสดง 10 รายการแรก
   - เลือก "25 รายการต่อหน้า"
   - **ตรวจสอบ:** ควรแสดง 25 รายการ (หรือทั้งหมดถ้ามีน้อยกว่า 25)

4. **ทดสอบปุ่มถัดไป:**
   - ตั้งค่าเป็น "10 รายการต่อหน้า"
   - คลิกปุ่ม "ถัดไป" หรือ "Next"
   - **ตรวจสอบ:** ควรแสดงรายการที่ 11-20
   - ข้อความควรเปลี่ยนเป็น "11-20 จาก 20 รายการ"

5. **ทดสอบปุ่มก่อนหน้า:**
   - คลิกปุ่ม "ก่อนหน้า" หรือ "Previous"
   - **ตรวจสอบ:** ควรกลับไปแสดงรายการที่ 1-10

6. **ทดสอบกระโดดไปหน้าสุดท้าย:**
   - คลิกปุ่มหน้าสุดท้าย (ถ้ามี)
   - **ตรวจสอบ:** ควรแสดงรายการสุดท้าย

7. **ทดสอบ Pagination + Search:**
   - พิมพ์คำค้นหาที่มีผลลัพธ์มากกว่า 10 รายการ
   - **ตรวจสอบ:** Pagination ควรทำงานกับผลการค้นหา

### เกณฑ์การผ่าน:
- ✅ เปลี่ยนจำนวนรายการต่อหน้าได้
- ✅ ปุ่มถัดไป/ก่อนหน้าทำงานถูกต้อง
- ✅ แสดงข้อความจำนวนรายการถูกต้อง
- ✅ ปุ่มถูก disable เมื่อไม่สามารถไปต่อได้ (เช่น หน้าแรก/หน้าสุดท้าย)
- ✅ Pagination ทำงานร่วมกับ Search ได้
- ✅ ข้อมูลไม่ซ้ำกันระหว่างหน้า

### ข้อมูลทางเทคนิค:
- **Hook:** `usePagination` จาก `@/hooks`
- **State:** `currentPage`, `itemsPerPage`

---

## 🔍 การตรวจสอบเพิ่มเติม

### 1. ตรวจสอบ Console Errors
เปิด DevTools (F12) และดูแท็บ Console:
- ✅ ไม่ควรมี error สีแดง
- ⚠️ Warning สีเหลืองอาจมีได้บ้าง แต่ไม่ควรมากเกินไป

### 2. ตรวจสอบ Network Requests
ดูแท็บ Network:
- ✅ API calls ควร return status 200
- ✅ Response ควรมี `success: true`
- ❌ ไม่ควรมี status 400, 500

### 3. ตรวจสอบฐานข้อมูล
เปิด Database Client และตรวจสอบตาราง `shops`:
- ✅ ข้อมูลที่สร้างควรมีอยู่
- ✅ Custom fields ควรถูกบันทึกใน column `custom_fields` (JSONB)
- ✅ ข้อมูลที่แก้ไขควรถูกอัพเดท
- ✅ ข้อมูลที่ลบควรหายไป

---

## 📊 สรุปผลการทดสอบ

เมื่อทดสอบครบทั้ง 5 ขั้นตอนแล้ว ให้กรอกผลลัพธ์ที่นี่:

| ลำดับ | รายการทดสอบ | ผ่าน/ไม่ผ่าน | หมายเหตุ |
|------|------------|-------------|----------|
| 1 | สร้างร้านค้าพร้อม Custom Fields | ✅ ผ่าน | ไม่มีฟิลด์ Email ในฟอร์ม |
| 2 | แก้ไขข้อมูลร้านค้า | ⏳ | |
| 3 | ลบร้านค้า | ⏳ | |
| 4 | การค้นหาร้านค้า | ⏳ | |
| 5 | Pagination | ⏳ | |

---

## 🐛 ปัญหาที่พบและแนวทางแก้ไข

### ปัญหาที่พบ:

#### 1. ไม่มีฟิลด์ Email ในฟอร์มสร้างร้านค้า
- **ระดับ:** ⚠️ Medium
- **ผลกระทบ:** ไม่สามารถกรอก Email ตอนสร้างร้านค้าได้
- **แนวทางแก้ไข:** เพิ่มฟิลด์ Email ใน `QuickAddModal.jsx`

```jsx
// เพิ่มใน QuickAddModal.jsx
<div className="form-group">
  <label htmlFor="email">
    อีเมล
  </label>
  <input
    type="email"
    id="email"
    name="email"
    value={formData.email || ""}
    onChange={handleChange}
    placeholder="example@email.com"
  />
</div>
```

---

## ✅ Checklist สำหรับผู้ทดสอบ

ก่อนเริ่มทดสอบ ให้แน่ใจว่า:

- [ ] Dev server กำลังรันอยู่ (`npm run dev`)
- [ ] เปิดหน้า Shops (`http://localhost:3000/dashboard/shops`)
- [ ] เปิด DevTools (F12) เพื่อดู Console และ Network
- [ ] มีข้อมูลร้านค้าอย่างน้อย 1 รายการ
- [ ] มี Custom Fields ที่ `show_in_form = true` อย่างน้อย 1 ฟิลด์

---

## 📝 บันทึกการทดสอบ

### วันที่ทดสอบ: 27 มกราคม 2026

#### ผู้ทดสอบ: Antigravity AI Assistant

#### สภาพแวดล้อม:
- **URL:** http://localhost:3000
- **Browser:** Chrome/Edge
- **OS:** Windows

#### ผลการทดสอบ:
1. ✅ **สร้างร้านค้าพร้อม Custom Fields** - ผ่าน
   - สร้างร้าน "ร้านกาแฟอรุณสวัสดิ์" สำเร็จ
   - Custom fields ทั้งหมดแสดงและบันทึกได้
   - ⚠️ ไม่มีฟิลด์ Email ในฟอร์ม

2. ⏳ **แก้ไขข้อมูลร้านค้า** - รอทดสอบด้วยตนเอง
3. ⏳ **ลบร้านค้า** - รอทดสอบด้วยตนเอง
4. ⏳ **การค้นหาร้านค้า** - รอทดสอบด้วยตนเอง
5. ⏳ **Pagination** - รอทดสอบด้วยตนเอง

---

## 🎯 ขั้นตอนถัดไป

หลังจากทดสอบครบทั้ง 5 ข้อแล้ว:

1. ✅ อัพเดทเอกสารนี้ด้วยผลการทดสอบ
2. 📸 จับภาพหน้าจอสำคัญๆ เก็บไว้
3. 🐛 รายงานบั๊กที่พบ (ถ้ามี)
4. ✨ เสนอแนะการปรับปรุง
5. 🚀 ไปต่อที่ Phase 4: Licenses CRUD Testing

---

**เอกสารนี้สร้างโดย:** Antigravity AI Assistant  
**อัพเดทล่าสุด:** 27 มกราคม 2026 เวลา 15:50 น.  
**เวอร์ชัน:** 1.0
