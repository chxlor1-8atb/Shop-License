# การผสานระบบ Custom Fields กับระบบสร้างร้านค้า

## 📋 สรุปการเปลี่ยนแปลง

ระบบสร้างร้านค้าใน **QuickAddModal** ได้รับการอัพเกรดให้รองรับ **Custom Fields** อย่างสมบูรณ์แล้ว! 

### ✨ ฟีเจอร์ใหม่

1. **โหลด Custom Fields อัตโนมัติ** - เมื่อเปิดฟอร์มสร้างร้านค้า ระบบจะดึง custom fields ที่กำหนดไว้มาแสดงโดยอัตโนมัติ
2. **รองรับหลายประเภทฟิลด์** - รองรับ text, number, date, textarea, และ select
3. **ฟิลด์บังคับ (Required)** - แสดงเครื่องหมาย * สำหรับฟิลด์ที่บังคับกรอก
4. **UI ที่สวยงาม** - มีส่วนแบ่งที่ชัดเจนระหว่างฟิลด์มาตรฐานและ custom fields
5. **บันทึกข้อมูลถูกต้อง** - แยกข้อมูล standard fields และ custom fields ก่อนส่งไปยัง API

---

## 🎯 วิธีใช้งาน

### 1. สร้าง Custom Fields สำหรับร้านค้า

ไปที่ **Settings > Custom Fields** และสร้างฟิลด์ใหม่:

```
Entity Type: shops
Field Name: cf_tax_id (ตัวอย่าง)
Field Label: เลขประจำตัวผู้เสียภาษี
Field Type: text
Show in Form: ✓ เปิด
Show in Table: ✓ เปิด
```

### 2. สร้างร้านค้าใหม่

1. ไปที่หน้า **Dashboard > Shops**
2. คลิกปุ่ม **"สร้างร้านค้าใหม่"**
3. กรอกข้อมูลพื้นฐาน:
   - ชื่อร้านค้า (บังคับ)
   - ชื่อเจ้าของ
   - เบอร์โทรศัพท์
   - ที่อยู่

4. **ส่วน Custom Fields** จะปรากฏด้านล่าง:
   - ระบบจะแสดงฟิลด์ที่คุณสร้างไว้
   - กรอกข้อมูลตามต้องการ
   - ฟิลด์ที่มีเครื่องหมาย * ต้องกรอก

5. (ตัวเลือก) เลือก **"สร้างใบอนุญาตพร้อมกัน"** หากต้องการ
6. คลิก **"บันทึก"**

---

## 🔧 รายละเอียดทางเทคนิค

### ไฟล์ที่แก้ไข

**`src/components/ui/QuickAddModal.jsx`**

#### การเปลี่ยนแปลงหลัก:

1. **เพิ่ม State สำหรับ Custom Fields**
```jsx
const [customFields, setCustomFields] = useState([]);
const [loadingFields, setLoadingFields] = useState(false);
```

2. **ฟังก์ชันดึงข้อมูล Custom Fields**
```jsx
const fetchCustomFields = async () => {
  const res = await fetch(`/api/custom-fields?entity_type=shops&t=${Date.now()}`);
  const data = await res.json();
  if (data.success) {
    const standardFields = ['shop_name', 'owner_name', 'phone', 'address', 'email', 'notes', 'license_count'];
    const fields = (data.fields || []).filter(
      f => f.show_in_form && !standardFields.includes(f.field_name)
    );
    setCustomFields(fields);
  }
};
```

3. **แยกข้อมูลก่อนส่ง API**
```jsx
const handleSubmit = async (e) => {
  if (type === "shop") {
    const standardFields = ['shop_name', 'owner_name', 'phone', 'address', 'email', 'notes', 'create_license', 'license_type_id', 'license_number'];
    const customFieldsData = {};
    
    Object.keys(formData).forEach(key => {
      if (!standardFields.includes(key)) {
        customFieldsData[key] = formData[key];
      }
    });
    
    const payload = {
      ...standardData,
      custom_fields: customFieldsData,
    };
    
    await onSubmit(payload);
  }
};
```

4. **Render Custom Fields แบบ Dynamic**
```jsx
{customFields.map((field) => {
  switch(field.field_type) {
    case 'textarea': return <textarea ... />;
    case 'number': return <input type="number" ... />;
    case 'date': return <DatePicker ... />;
    case 'select': return <CustomSelect ... />;
    default: return <input type="text" ... />;
  }
})}
```

---

## 📊 โครงสร้างข้อมูล

### ข้อมูลที่ส่งไปยัง API

```json
{
  "shop_name": "ร้านค้าตัวอย่าง",
  "owner_name": "นายตัวอย่าง",
  "phone": "081-234-5678",
  "address": "123 ถนนตัวอย่าง",
  "email": "example@email.com",
  "notes": "หมายเหตุ",
  "custom_fields": {
    "cf_tax_id": "1234567890123",
    "cf_business_type": "ร้านค้าปลีก",
    "cf_registration_date": "2024-01-15"
  }
}
```

### ข้อมูลที่บันทึกในฐานข้อมูล

ตาราง `shops`:
- `custom_fields` (JSONB) - เก็บข้อมูล custom fields ทั้งหมด

---

## ✅ การทดสอบ

### ทดสอบการทำงาน:

1. ✅ สร้าง custom field ใหม่ในหน้า Settings
2. ✅ เปิดฟอร์มสร้างร้านค้า - ต้องเห็น custom field ที่สร้าง
3. ✅ กรอกข้อมูลและบันทึก - ข้อมูลต้องถูกบันทึกในฐานข้อมูล
4. ✅ ดูข้อมูลในตาราง - custom fields ต้องแสดงในคอลัมน์
5. ✅ แก้ไขข้อมูลในตาราง - ต้องสามารถแก้ไข custom fields ได้

### ทดสอบ Field Types:

- ✅ **Text** - กรอกข้อความปกติ
- ✅ **Number** - กรอกตัวเลขเท่านั้น
- ✅ **Date** - เลือกวันที่จาก DatePicker
- ✅ **Textarea** - กรอกข้อความหลายบรรทัด
- ✅ **Select** - เลือกจาก dropdown พร้อมค้นหา

---

## 🎨 UI/UX Improvements

1. **ส่วนแบ่งที่ชัดเจน** - มีเส้นแบ่งและหัวข้อ "ข้อมูลเพิ่มเติม (Custom Fields)"
2. **ไอคอนสวยงาม** - ใช้ `fa-sliders-h` แสดงส่วน custom fields
3. **Required Indicator** - แสดง * สีแดงสำหรับฟิลด์บังคับ
4. **Placeholder ที่เหมาะสม** - แต่ละฟิลด์มี placeholder ที่บอกให้กรอกอะไร
5. **Responsive** - ทำงานได้ดีทั้งบนมือถือและคอมพิวเตอร์

---

## 🔄 การทำงานร่วมกับระบบอื่น

### 1. ระบบตาราง (ExcelTable)
- ✅ Custom fields แสดงเป็นคอลัมน์ในตาราง
- ✅ สามารถแก้ไขได้โดยตรงในตาราง
- ✅ เพิ่ม/ลบคอลัมน์ได้

### 2. ระบบ API
- ✅ GET `/api/shops` - ดึงข้อมูล custom_fields มาด้วย
- ✅ POST `/api/shops` - บันทึก custom_fields
- ✅ PUT `/api/shops` - อัพเดท custom_fields
- ✅ DELETE `/api/shops` - ลบพร้อม custom_fields

### 3. ระบบ Custom Fields Management
- ✅ สร้าง/แก้ไข/ลบ custom fields ได้
- ✅ กำหนด field type, required, show_in_form, show_in_table
- ✅ จัดเรียงลำดับการแสดงผล

---

## 🚀 ประโยชน์ที่ได้รับ

1. **ความยืดหยุ่น** - เพิ่มฟิลด์ใหม่ได้โดยไม่ต้องแก้โค้ด
2. **ประหยัดเวลา** - ไม่ต้องสร้างฟอร์มใหม่ทุกครั้ง
3. **ใช้งานง่าย** - UI ที่เข้าใจง่าย ไม่ซับซ้อน
4. **ปลอดภัย** - ตรวจสอบ required fields อัตโนมัติ
5. **รองรับอนาคต** - เพิ่มฟีเจอร์ใหม่ได้ง่าย

---

## 📝 ตัวอย่างการใช้งานจริง

### กรณีที่ 1: ร้านค้าต้องการเก็บเลขประจำตัวผู้เสียภาษี

1. สร้าง custom field:
   - Field Name: `cf_tax_id`
   - Field Label: `เลขประจำตัวผู้เสียภาษี`
   - Field Type: `text`
   - Required: `true`

2. เมื่อสร้างร้านค้าใหม่ จะมีฟิลด์นี้ปรากฏในฟอร์ม
3. ข้อมูลจะถูกบันทึกและแสดงในตาราง

### กรณีที่ 2: ต้องการเก็บวันที่จดทะเบียน

1. สร้าง custom field:
   - Field Name: `cf_registration_date`
   - Field Label: `วันที่จดทะเบียน`
   - Field Type: `date`

2. ใช้ DatePicker เลือกวันที่
3. ข้อมูลจะแสดงในรูปแบบวันที่ที่อ่านง่าย

---

## 🐛 การแก้ปัญหา

### ปัญหา: Custom fields ไม่แสดงในฟอร์ม

**วิธีแก้:**
1. ตรวจสอบว่า `show_in_form = true` ใน custom_fields table
2. ตรวจสอบว่า `entity_type = 'shops'`
3. ลอง refresh หน้าใหม่

### ปัญหา: ข้อมูลไม่ถูกบันทึก

**วิธีแก้:**
1. เปิด DevTools > Network > ดู request payload
2. ตรวจสอบว่า `custom_fields` object มีข้อมูลครบ
3. ตรวจสอบ API response

### ปัญหา: Select field ไม่มี options

**วิธีแก้:**
1. ตรวจสอบ `field_options` ใน custom_fields table
2. ต้องเป็น JSON array: `["option1", "option2"]`
3. หรือ: `[{"value": "1", "label": "Option 1"}]`

---

## 📚 เอกสารเพิ่มเติม

- [Custom Fields API Documentation](./API_DOCUMENTATION.md)
- [Database Schema](./schema.sql)
- [Component Documentation](./COMPONENTS.md)

---

**อัพเดทล่าสุด:** 27 มกราคม 2026  
**เวอร์ชัน:** 2.0  
**ผู้พัฒนา:** Antigravity AI Assistant
