/**
 * Supabase Client - DISABLED
 * 
 * This project now uses Neon database instead of Supabase
 * Supabase client is disabled to prevent connection errors
 */

// Export null to prevent import errors
export const supabase = null;
